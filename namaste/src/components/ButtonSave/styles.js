import styled from 'styled-components';

export const Button = styled.button`
    border: 0;
    background: transparent;
    cursor: pointer;

    p.salvar {
        font-size: 14px;
        font-family: 'Roboto', sans-serif;
        font-weight: 500;
        border-bottom: 2px solid #009d70;
        color: #009d70;
        text-transform: uppercase;
    }
`;
