import styled from 'styled-components';

export const Button = styled.button`
    border: 0;
    background: transparent;
    cursor: pointer;

    p.cancelar {
        font-size: 14px;
        font-family: 'Roboto', sans-serif;
        font-weight: 500;
        border-bottom: 2px solid #fc5368;
        color: #fc5368;
        text-transform: uppercase;
    }
`;
